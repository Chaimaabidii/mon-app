name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Railway CLI
        run: |
          bash -c "$(curl -sSL https://railway.app/cli.sh)"

      - name: Set up Railway API Token
        run: echo "RA
